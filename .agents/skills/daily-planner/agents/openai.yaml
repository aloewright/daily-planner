interface:
  display_name: "Daily Planner"
  short_description: "Repo-specific patterns and workflows for daily-planner"
  default_prompt: "Use the daily-planner repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true